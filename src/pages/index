import { Link } from "react-router-dom";
import { useProducts } from "@/hooks/useProducts";
import { Skeleton } from "@/components/ui/skeleton";
import heroBg from "@/assets/hero-bg.jpg";
 
const CatalogPage = () => {
  const { data: products, isLoading } = useProducts();
 
  return (
    <div className="min-h-screen bg-background">
      {/* Nav */}
      <nav className="fixed top-0 left-0 right-0 z-50 bg-background/80 backdrop-blur-md border-b border-gold">
        <div className="container mx-auto flex items-center justify-between py-4 px-6">
          <Link to="/" className="gradient-gold-text text-xl font-bold tracking-[0.4em] uppercase">
            Luxe
          </Link>
          <div className="flex items-center gap-6">
            <a href="#collection" className="text-xs tracking-[0.2em] uppercase text-muted-foreground hover:text-gold transition-colors duration-300">
              Coleção
            </a>
            <Link
              to="/admin"
              className="text-xs tracking-[0.2em] uppercase text-muted-foreground hover:text-gold transition-colors duration-300"
            >
              Admin
            </Link>
          </div>
        </div>
      </nav>
 
      {/* Hero */}
      <section className="relative h-[80vh] flex items-center justify-center overflow-hidden">
        <img
          src={heroBg}
          alt=""
          className="absolute inset-0 w-full h-full object-cover"
          width={1920}
          height={1080}
        />
        <div className="absolute inset-0 bg-gradient-to-b from-background/40 via-transparent to-background" />
        <div className="relative z-10 text-center px-4">
          <div className="w-16 h-[1px] gradient-gold mx-auto mb-8" />
          <h1 className="text-5xl md:text-7xl font-extralight tracking-[0.15em] text-foreground mb-6">
            Nossa <span className="gradient-gold-text font-light">Coleção</span>
          </h1>
          <p className="text-muted-foreground font-light max-w-lg mx-auto text-sm tracking-wide leading-relaxed">
            Produtos selecionados com excelência, sofisticação e atenção a cada detalhe.
          </p>
          <div className="w-16 h-[1px] gradient-gold mx-auto mt-8" />
        </div>
      </section>
 
      {/* Products */}
      <section id="collection" className="container mx-auto px-6 py-20">
        <div className="flex items-center gap-4 mb-12">
          <div className="h-[1px] flex-1 bg-gradient-to-r from-transparent to-border" />
          <h2 className="text-xs tracking-[0.4em] uppercase text-gold">Produtos</h2>
          <div className="h-[1px] flex-1 bg-gradient-to-l from-transparent to-border" />
        </div>
 
        {isLoading ? (
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {Array.from({ length: 6 }).map((_, i) => (
              <div key={i} className="space-y-4">
                <Skeleton className="aspect-[3/4] w-full bg-secondary" />
                <Skeleton className="h-4 w-2/3 bg-secondary" />
                <Skeleton className="h-4 w-1/3 bg-secondary" />
              </div>
            ))}
          </div>
        ) : products && products.length > 0 ? (
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {products.map((product, i) => (
              <Link
                key={product.id}
                to={`/product/${product.id}`}
                className="group relative animate-fade-in"
                style={{ animationDelay: `${i * 100}ms` }}
              >
                <div className="relative aspect-[3/4] overflow-hidden bg-secondary border border-border hover:border-gold transition-all duration-500">
                  {product.image_url ? (
                    <img
                      src={product.image_url}
                      alt={product.name}
                      loading="lazy"
                      className="w-full h-full object-cover transition-all duration-700 group-hover:scale-105 group-hover:brightness-110"
                    />
                  ) : (
                    <div className="w-full h-full flex items-center justify-center shimmer">
                      <span className="text-5xl font-extralight gradient-gold-text">{product.name[0]}</span>
                    </div>
                  )}
                  {/* Overlay */}
                  <div className="absolute inset-0 bg-gradient-to-t from-background/90 via-background/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 flex items-end p-6">
                    <span className="text-xs tracking-[0.3em] uppercase text-gold">Ver detalhes →</span>
                  </div>
                </div>
                <div className="mt-4 space-y-1">
                  <h3 className="font-light text-foreground tracking-wider text-sm uppercase group-hover:text-gold transition-colors duration-300">
                    {product.name}
                  </h3>
                  <div className="flex items-center justify-between">
                    {product.price && (
                      <p className="text-gold text-sm font-light">
                        R$ {product.price.toFixed(2)}
                      </p>
                    )}
                    {product.category && (
                      <p className="text-[10px] text-muted-foreground tracking-[0.3em] uppercase">
                        {product.category}
                      </p>
                    )}
                  </div>
                </div>
              </Link>
            ))}
          </div>
        ) : (
          <div className="text-center py-24">
            <div className="w-20 h-20 mx-auto mb-6 rounded-full border border-gold flex items-center justify-center glow-gold">
              <span className="gradient-gold-text text-2xl">✦</span>
            </div>
            <p className="text-muted-foreground font-light text-lg mb-2">
              Nenhum produto disponível
            </p>
            <p className="text-muted-foreground/60 text-sm">
              Novidades em breve.
            </p>
          </div>
        )}
      </section>
 
      {/* Footer */}
      <footer className="border-t border-border py-12">
        <div className="container mx-auto px-6 flex flex-col items-center gap-4">
          <span className="gradient-gold-text text-sm font-bold tracking-[0.4em] uppercase">Luxe</span>
          <div className="w-8 h-[1px] gradient-gold" />
          <p className="text-[10px] text-muted-foreground tracking-[0.3em] uppercase">
            © {new Date().getFullYear()} — Todos os direitos reservados
          </p>
        </div>
      </footer>
    </div>
  );
};
 
export default CatalogPage;
